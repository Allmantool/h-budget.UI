#!/usr/bin/env sh

# Commitlint needs Git's message-file argument, which is only available in the
# commit-msg hook. Keep this hook inert instead of attempting a second, dynamic
# package-runner invocation with an empty argument.
exit 0
